import styled from "styled-components";

const StyledForm = styled.div`
  height: 200vh;
`;

const Form = () => {
  return (
    <StyledForm className="tagline-mission-statement-form">Form</StyledForm>
  );
};

export default Form;
